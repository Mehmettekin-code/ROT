{
  "name": "my-vercel-app",
  "version": "1.0.0",
  "description": "Vercel deploy with MongoDB",
  "type": "module",
  "scripts": {
    "build": "echo 'No build step required'",
    "start": "node api/test.js"
  },
  "dependencies": {
    "mongodb": "^6.9.0"
  }
}
